[package]
name = "wasi-nn-torch"
version = "0.1.0"
edition = "2021"
description = "Minimal wasi-nn TorchScript runner — exercises an XOR MLP via WACS's TorchSharp backend on wasm32-wasip2."
publish = false

[[bin]]
name = "wasi-nn-torch"
path = "src/main.rs"

[dependencies]
wit-bindgen = "0.57"
# Profiles are defined at the workspace root.
